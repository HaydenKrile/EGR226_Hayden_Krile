#include "msp.h"


/**
 * main.c
 */
//uint8_t DebounceSwitch1(void);

void main(void)
{

    P2->SEL1 &= ~1;
    P2->SEL0 &= ~1;
    P2->DIR |= 0x07;

    while(1){

        P2->OUT |= 1;
        __delay_cycles(300000);
        P2->OUT &= ~1;
        __delay_cycles(300000);
    }

}
